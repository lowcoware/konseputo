---
title: Decisions
description: Annotated feed of landed ADRs — the "why" behind the suite's structural calls.
---

# Decisions

Not a duplicate of a full ADR — one annotated line per landed decision,
linking to the real ADR for the full Context/Alternatives/Consequences.

- **ADR-001 — Every fixed-form generator ships a self-generated reference
  artifact.** Landed 2026-07-24. `konseputo-artifact` was calibrating
  against a vendored third-party gallery, never its own output — nothing
  caught drift between "the rule as written" and "the rule as executed."
  Full ADR: `skills/konseputo-project-management/references/self-reference.md`.
  This deep-dive wiki you're reading is `konseputo-wiki`'s own instance of
  that rule, rebuilt as a real multi-page site instead of a single
  index note once it became clear a team-facing wiki needs pages, not
  just a map.

Back to [[_MOC_Reference]]
