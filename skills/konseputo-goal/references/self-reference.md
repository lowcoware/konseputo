# Self-reference artifact — ROADMAP + STATE, built by this skill's own templates

Self-generated reference for `konseputo-goal`: `ROADMAP.md` + `STATE.md`
filled from this skill's own templates, documenting a real run — the
reference-artifact directive's rollout across the six named candidate
skills — not a fabricated example. Placeholders (`{{...}}`) resolved with
actual values from this session, same as a real dispatch would.

---

## ROADMAP.md

# Roadmap: Self-generated reference artifacts

**Task:** Every fixed-form-output skill ships a reference artifact it generated itself, inside its own `references/` folder.
**Type:** cross-cutting, no-new-code, docs-and-examples
**Created:** 2026-07-24
**Total phases:** 6

### Context summary

- **Stack:** Markdown/HTML skill-authoring, no build step, no runtime
- **Package manager:** n/a
- **Build / test / lint commands:** `node scripts/check-skills.js`, `node scripts/check-sync.js`, `node scripts/check-versions.js`
- **Risky areas:** a reference that looks plausible but wasn't actually produced by the skill's own rules defeats the whole point — each phase must cite which rule/template produced its artifact, not just resemble one

### Assumptions

- konseputo-artifact's `self-reference.html` (done before this ROADMAP existed) sets the acceptance bar for the rest — check-skills.js green, no dangling references, genuinely regenerable.
- `konseputo-md-generator`'s existing `showcase.md` already satisfies the directive — its phase is verification, not net-new generation.

### Risk top 3

1. **A reference silently drifts from its skill's current rules** — likelihood: high over months, mitigation: `konseputo-debt` should eventually track this; not solved by this run.
2. **Fabricating a plausible-looking artifact instead of genuinely applying the skill** — likelihood: medium, mitigation: every phase must show its work (which template, which real fact, not invented content).
3. **check-skills.js's bare-reference parser false-positives on prose mentioning a filename** (a suite-frontend suffix, an agent-instructions filename, and a backlog filename all tripped it earlier this session) — likelihood: high, mitigation: rephrase prose to avoid the literal pattern, verified per-phase.

### Phase map

| # | Phase | Depends on | Deliverable |
|---|-------|------------|-------------|
| 1 | konseputo-artifact | — | `references/self-reference.html` — mission-control then Apple then Apple/lowcoware-blend redesign, bugs fixed live in-browser |
| 2 | konseputo-wiki | — | `references/self-reference.md` — a real Skills MOC file via this skill's own `moc.md` workflow |
| 3 | konseputo-md-generator | — | verified `showcase.md` already satisfies the directive; SKILL.md note added, no new file |
| 4 | konseputo-project-management | — | `references/self-reference.md` — ADR-001, this skill's own `adr.md` template, documenting the directive itself |
| 5 | konseputo-goal | 1-4 (as source material) | this file — `ROADMAP.md` + `STATE.md` documenting phases 1-4 as a real completed run |
| 6 | Polish & Harden | 1-5 | konseputo-clone reference (candidate, not yet started at time of writing); `check-skills.js`/`check-sync.js`/`check-versions.js` clean across all touched skills |

---

### Phase 1 — konseputo-artifact

**Why:** the directive's own motivating example — this skill measured itself against a vendored gallery, not its own output.

**Deliverables:**
- `skills/konseputo-artifact/references/self-reference.html`

**Acceptance criteria:**
- [x] Lives inside the skill, not linked out
- [x] Generated by the skill's own genre/palette/dark-mode rules
- [x] Verified rendering in a real browser (chrome-devtools MCP), bugs found and fixed live: missing number-circle badges, baseline-alignment shift, arrow convergence geometry, font stack silently not loading, diagram not recentring on resize

**Mandatory commands:**
- `node scripts/check-skills.js`

**Evidence required:**
- Screenshot diff before/after each bug fix (not persisted as a file — verified in-session via chrome-devtools MCP screenshots)

### Phase 2 — konseputo-wiki

**Why:** MOC-building is this skill's most-used fixed output shape.

**Deliverables:**
- `skills/konseputo-wiki/references/self-reference.md`

**Acceptance criteria:**
- [x] Follows `moc.md`'s exact format: title, one-sentence intro, `## Category` clusters, `[[wikilink]]` + one annotation per note
- [x] Indexes the suite's real 22 skills, not placeholder content

### Phase 3 — konseputo-md-generator

**Why:** check whether the directive was already satisfied before generating anything new.

**Deliverables:** none new — `references/showcase.md` (pre-existing) confirmed to already meet both conditions.

**Acceptance criteria:**
- [x] Verified `showcase.md` has real frontmatter, exercises real syntax, is openable in Obsidian as a self-check
- [x] SKILL.md's reference-table entry updated to state this explicitly, so the gap doesn't get "rediscovered" later

### Phase 4 — konseputo-project-management

**Why:** ADR is this skill's flagship fixed-form artifact.

**Deliverables:**
- `skills/konseputo-project-management/references/self-reference.md`

**Acceptance criteria:**
- [x] Follows `adr.md`'s template exactly: Status/Context/Decision/Alternatives/Consequences/Links
- [x] Documents a REAL decision (this directive), not an invented one

## STATE.md

**Status:** IN_PROGRESS
**Current phase:** 5 (this phase)
**Started:** 2026-07-24
**Last update:** 2026-07-24
**Run root:** (no `.konseputo-goal/` run directory — this rollout ran as direct conversation turns, not a dispatched `/goal` session; documented here as what the ROADMAP/STATE shape would have captured had it been one)
**Baseline ref:** commit at start of the reference-artifact rollout (see repo log around the konseputo-artifact self-reference commits)

### Phase progress

| # | Phase | Status | Started | Completed | Notes |
|---|-------|--------|---------|-----------|-------|
| 1 | konseputo-artifact | done | 2026-07-24 | 2026-07-24 | multiple live redesign + bugfix rounds, verified in-browser |
| 2 | konseputo-wiki | done | 2026-07-24 | 2026-07-24 | — |
| 3 | konseputo-md-generator | done | 2026-07-24 | 2026-07-24 | no new file — existing showcase.md already qualified |
| 4 | konseputo-project-management | done | 2026-07-24 | 2026-07-24 | — |
| 5 | konseputo-goal | in_progress | 2026-07-24 | — | this file |
| 6 | Polish & Harden | pending | — | — | konseputo-clone still open |

### Engineering check status

- Build: n/a (no build step)
- Typecheck: n/a
- Lint: —
- Tests: `check-skills.js` / `check-sync.js` / `check-versions.js` — green after every phase

### Notable events

- 2026-07-24 — Plan locked, 6 phases.
- 2026-07-24 — Phase 1 went through three visual redesigns (Mission Control -> Apple -> Apple/lowcoware blend) plus 4 rounds of browser-verified bug fixes, well beyond a single-pass generation — kept as evidence that "generated by the skill" survives real iteration, not just a first draft.
- 2026-07-24 — check-skills.js's bare-reference parser false-positived three separate times this rollout (a suite-frontend suffix, an agent-instructions filename, a backlog filename) — each fixed by rephrasing the triggering prose, not by weakening the check.

### Failure log

No phase hit a FAILURE_PROBE. The three parser false-positives above were caught by the mandatory command (`check-skills.js`) and fixed inline within the same phase — exactly the self-heal behavior `execution.md` describes, working as designed on a real (non-dispatched) run.
